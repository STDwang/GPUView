# GPUView 项目协作约定

项目目标：面向 GPU 性能分析工具 UI 岗位的可运行演示与学习材料。项目名称 GPUView，用户已授权公开 Git 仓库及后续项目更新。

## 开发要求

- C++17 / Qt 6 Widgets / CMake；模块按 core、adapters、application、ui_widgets 分层。核心算法可脱离界面测试，UI不直接解析文件。
- 接口、所有权、线程边界、单位转换、性能取舍等必要位置写中文注释；解释为什么，不逐句翻译代码。
- 每项功能同步更新 README 中的真实完成状态、运行方式和限制，以及对应测试、面试题讲解和代码链接。不要把规划写成已实现。
- 用户以学习和能对代码讲解为目标，不要求所有代码由用户手写。Quick阶段也是技术对照学习，可由AI辅助。
- 面试题清单以 docs/07-interview/QUESTIONS.md 为准。答案应有问题背景、可复现情景、解决思路、代码例子、测试/测量证据和不足。
- 性能数字只来自真实测量，合成数据醒目标识；帧数据不能冒充Kernel/调用栈/指令数据。
- Qt对象线程归属明确；工作线程不访问UI；取消、关闭、迟到结果必须验证。
- 每次实质改动后更新 docs/00-review/STATUS.md，便于额度中断后接续。不设固定周工时，不自行创建定时任务。

## 发布边界

- 公开源码、通用技术文档、可公开测试数据和必要的面试材料；个人履历、聊天链接、完整本机路径、原始采集、凭据不上传。
- .gitignore 中的本地资料仍保留在项目目录，公开文档不得依赖不可访问的本地资料。
- 推送前检查暂存文件、测试结果和README。不要强推或覆盖已有远端历史；如仓库已存在先检查。

## 当前批准范围

用户已确认 A真实帧数据闭环、B完整Widgets面试版、独立Qt6工具链、真实帧+教学Trace。后续按任务包推进；范围外扩展单独记录，不重新索要已授权的普通开发权限。
