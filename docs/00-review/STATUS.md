# 项目状态与接续入口

更新时间：2026-09-18。需求已确认，允许开发与公开同步。

## 本次完成

滚动条视觉调整：20px宽、深色滑槽、浅蓝灰实心滑块，悬停更亮、按下青绿色；移除细小箭头按钮，保留拖动、点击滑槽翻页和滚轮操作。样式限定在轨道滚动条。

轨道滚动条移至时间轴右侧、事件侧栏左侧，改为竖向；绘制与滚动共用可见行数，调整窗口高度时自动收紧末页范围。新增布局位置、末页缩放与清空数据的交互回归测试。

本次Debug构建及32个实际用例全部通过；本次未重跑Release。

最新目录调整：VS2022的GPUView.sln、GPUView.vcxproj、GPUView.sources.props、GPUView.vcxproj.filters均移至项目根目录；源文件和调试/输出路径同步改为相对根目录，生成脚本不再创建ide目录。

最新增量：支持VS Code/VS2022调试，CMake按模块目录自动收集源码。公开VS入口GPUView.sln使用相对源码路径和工程宏，后台仍调用CMake；build内生成工程为本机缓存，不提交。入口文件移动到src/app、tests/core、tests/ui、tools/benchmark，文档链接同步调整。调试构建自动部署Qt运行库，Debug/Release测试日志分离。

验证：Debug与Release均31个实际用例通过；VS入口MSBuild构建、文件增删自动发现、含空格目录迁移构建、Debug本地依赖启动均成功。PDB存在。IDE交互式断点未做UI自动化；项目F5配置已提供。

- 更新用户决策：学习和面试讲解优先，不要求代码全部亲手编写。
- 项目协作要求写入AGENTS.md；十题清单与CASEBOOK关联模块、代码、测试。
- 独立Qt6.8.3 SDK、VS2022/CMake构建及Qt Test。
- v0.1可运行教学内核：100k/1m合成事件、64轨道、区间索引、LOD、快照、缓存、原子进度、最新请求、取消/安全退出、时钟映射算法。
- 基础GUI：时间轴缩放/平移/框选/拾取，后台任务按钮与诊断栏。
- Release查询基准、原始CSV、截图、README与构建说明。

## 范围与限制

A/B完整产品未完成：真实PresentMon CSV、帧统计、表格/热力图、资源遥测、多源集成、文件导出、Trace文件解析、回放、干净系统部署均尚待实施。时钟映射只有算法测试，不是已完成真实CPU/GPU对齐。

当前测试验证教学内核，不代表PRD全部P0通过；性能结果是查询对照，不是端到端FPS。用户尚未完成口述/修改练习，不对掌握程度下结论。

## 下一任务

T01后半 + T02/T03：用明确目标程序取得短时PresentMon样本，核验表头/时间/缺失字段，建立适配器与小型夹具；随后做精确帧统计和异步文件加载。保持core与UI解耦，不把解析放进窗口。

之后每次功能提交同步更新README、CASEBOOK、测试与此文件。原始私人资料留本地，公开资料不依赖个人上下文。
